rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    // DESARROLLO: Permitir todo lo autenticado
    // En producción, usar reglas restrictivas
    match /{document=**} {
      allow read, write: if request.auth != null;
    }
  }
}

